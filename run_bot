#!/bin/bash
restartChannel=$1

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd $DIR
git pull origin master

npm install .

ts=$(date +%s)
pkill -f "nodejs bot.js"
nodejs bot.js $1 > logs/platbot-$ts.log 2>&1 &

echo "Plat-bot running."
